@model QuizApp.Models.Question
@{
    ViewData["Title"] = "Question Details";
}
<h2>@Model.Text</h2>
<ul>
    @foreach(var answer in Model.Answers)
    {
        <li>@answer.Text</li>
    }
</ul>
<p><a href="@Url.Action("Index", "Quiz")">Back to Quiz Overview</a></p>